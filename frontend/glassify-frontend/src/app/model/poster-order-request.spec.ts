import { PosterOrderRequest } from './poster-order-request';

describe('PosterOrderRequest', () => {
  it('should create an instance', () => {
    expect(new PosterOrderRequest()).toBeTruthy();
  });
});
