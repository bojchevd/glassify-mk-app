import { CustomPhotoOrderRequest } from './custom-photo-order-request';

describe('CustomPhotoOrderRequest', () => {
  it('should create an instance', () => {
    expect(new CustomPhotoOrderRequest()).toBeTruthy();
  });
});
